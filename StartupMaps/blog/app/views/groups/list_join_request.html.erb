<!-- # Definition: lists the requesters and has buttons to accept or reject the request.
     # input: requester and group_id.
     # output: requester and group_id.
     # Author: Sherouk A.Said.-->
  <table>
    <tr>
      <th>Name</th>
    </tr>
    <% @requester.each do |r| %>
      <tr>
        <td><%= r.name %></td>
        <td><%= link_to 'accept', group_accept_join_request_path(requester: r)%></td>
        <td><%= link_to 'reject', group_reject_join_request_path(requester: r)%></td>
      </tr>
    <% end %>
  </table>