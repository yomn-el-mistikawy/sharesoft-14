<%= form_for(@entity_portfolio) do |f| %>
  <% if @entity_portfolio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@entity_portfolio.errors.count, "error") %> prohibited this entity_portfolio from being saved:</h2>

      <ul>
      <% @entity_portfolio.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :launch %><br>
    <%= f.datetime_select :launch %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
