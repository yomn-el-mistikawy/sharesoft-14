<!--
  # Definition: "A startup can see a list of his projects" 
  # This method allows you to get a list of projects. 
  # that belong to a specific startup with all its categories
  # link_to, it gives it the privileges to link it to the project.
  # Input: entity_id, startup_id.
  # Output: project_id "all project description in a list".
  # Author: Hana Magdy. -->

<table>
 <thead>
   <tr>
     <th width="12%", align="left">Name</th>
     <th width="12%", align="left">Description</th>
   </tr>
 </thead>
  <tbody>
    <% @projects.each do |project| %>
      <tr>
       	<td width="22%"> <%= link_to project.name, project %> </td>
       	<td width="22%"> <%= project.description %> </td> 
      </tr>
    <% end %>
  </tbody>
</table>
<% if current_entity.type == "Startup" %>
  <%= link_to "Create new project", new_project_path, :class => "button" %>
<% end %>