 
<% @entities.each do |entity| %>
  <tr>
    <td><%= entity.title %></td>
    <td><%= entity.text %></td>
    <td><%= link_to 'Show', start %></td>
    <td><%= link_to 'Edit', edit_entity_path(entity) %></td>
  </tr>
<% end %>